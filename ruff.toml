line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# E741: `l` is the standard PDF coordinate name (left).
# UP037: keep stringified self-references for readability.
ignore = ["E501", "E741", "UP037"]

[lint.per-file-ignores]
"tests/**" = ["B", "SIM"]
